# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 안내서다.

## 프로젝트 개요

넥슨 게임잼(4인 팀 / 2박 3일) 출품작. 주제는 **"맥락(Context)"**, 그중에서도 **"시대적 맥락"**.
"만져라! 메이드인 와리오" 스타일의 초고속 미니게임 컬렉션이며, 빅뱅 → 선사 → 중세 → 근대 → 현대 → 미래
순서로 시대별 미니게임을 클리어해 나간다. 자세한 내용은 아래 [게임 기획서](#게임-기획서-docs기획md) 참고.

- **엔진:** Unity 6000.3.7f1 (URP, 2D)
- **입력:** Unity Input System (`com.unity.inputsystem`), WASD / 방향키 기반
- **타겟:** Windows
- **주 브랜치:** `main` (작업은 개인 브랜치에서 → PR)

## 프로젝트 구조

```
Assets/
  00_Scenes/          씬. 00000_Player.unity 가 미니게임 재생 호스트 씬, 00_BigBang.unity 가 시작 씬
  01_Scripts/
    Core/             미니게임 프레임워크 (MiniGame, MiniGamePlayer, GameFlowController)
    Ending/           엔딩 크레딧·이미지 LLM 생성 (RunResult, EndingCreditController)
    Gallery/          엔딩 영구 보관 & 열람 (GalleryStore, GalleryScreen)
    Mini_BigBang/     빅뱅 프롤로그 (W 홀드로 우주 팽창 + 영상 스크럽)
    Mini_Fire/        선사시대 불 붙이기
    Mini_sha/         비비기(Rub) 미니게임
    Mini_witch/       마녀 찾기 (4x4 그리드)
    Mini_dosirak/     도시락 폭탄 던지기
  02_Sprite/          미니게임별 스프라이트 (01_Fire, 02_Sha, 03_witchCatch, 04_dosirakBomb)
  03_Prefabs/         미니게임 프리팹 (RubGame, WitchFindGame, DosirakBomb)
  04_Video/           영상 에셋 (빅뱅 영상 등)
docs/
  기획.md              게임 기획서 (원본)
  MiniGame_Contract.md 미니게임 개발 계약 — 새 미니게임을 만들기 전 반드시 읽을 것
  BigBang_MiniGame.md  빅뱅 미니게임 구현 문서 (영상 스크럽 방식, 씬/프리팹 구성)
  Ending_LLM.md        엔딩 크레딧·이미지 런타임 생성 (API 키, 조합 키, 폴백)
  Gallery.md           갤러리 저장 구조와 씬 구성
```

## 핵심 아키텍처

미니게임은 **프리팹 단위**로 만들고, 씬에 직접 배치하지 않는다.

- [MiniGame.cs](Assets/01_Scripts/Core/MiniGame.cs) — 모든 미니게임의 추상 베이스.
  `Play()` / `StopAndReset()` / `Finished` 이벤트를 제공하고, 파생 클래스는 `OnPlay()` / `OnStopAndReset()`
  두 훅만 구현한다. 종료 통지는 `ReportFinished(bool success)`로만 한다.
- [MiniGamePlayer.cs](Assets/01_Scripts/Core/MiniGamePlayer.cs) — 프리팹들을 비활성 홀더(`[Preload Pool]`)
  아래에 미리 `Instantiate` 해두고, `PlayGame(index)` / `PlayGame(prefab)` 으로 꺼내 재생한다.
  게임이 끝나면 `onGameFinished` 발화 → 자동으로 `StopAndReset()` + 비활성화 후 풀로 반환.
- [GameFlowController.cs](Assets/01_Scripts/Core/GameFlowController.cs) — 인스펙터에 등록한 순서대로
  미니게임을 재생한다. `isCritical` 로 표시된 게임에 실패하면 `onGameEnding` 을 발화하고 흐름을 중단하며,
  일반 게임은 실패해도 다음으로 진행한다. 전부 통과하면 `onAllGamesCleared`.

## 새 미니게임을 만들 때

**반드시 [docs/MiniGame_Contract.md](docs/MiniGame_Contract.md) 를 먼저 읽는다.** 요약하면:

1. `MiniGame` 을 상속하고 `OnPlay()` / `OnStopAndReset()` 만 구현한다.
2. 클리어/실패 시 `ReportFinished(true/false)` 를 정확히 한 번 호출한다. `Finished` 를 직접 발화하지 않는다.
3. `Awake` / `Start` 에서 게임을 자동 시작하지 않는다. `Update()` 는 자체 상태(`state == Playing`)로 가드한다.
4. `OnStopAndReset()` 은 멱등해야 한다 — 재생 중이 아닐 때 호출돼도 초기 상태로 수렴.
5. 프리팹에 `Main Camera` / `EventSystem` 을 넣지 않는다. 카메라는 직렬화 필드 + `Camera.main` 폴백.
6. `MiniGamePlayer.gamePrefabs` 에 등록해 재생 테스트한다.

## 코드 컨벤션

- 주석과 XML 문서 주석은 **한국어**로 작성한다. 기존 스크립트의 밀도와 어투를 따른다.
- 직렬화 필드는 `[SerializeField] private` + `[Header]` / `[Tooltip]` 로 인스펙터에서 읽히게 만든다.
- 제네릭 `UnityEvent<T>` 는 Unity가 직렬화하지 못하므로 구체 타입 서브클래스로 선언한다:
  ```csharp
  [Serializable] public class TimerEvent : UnityEvent<float> { }
  ```
- 미니게임 내부 상태는 별도 `State` enum 으로 관리하고, `IsPlaying` 은 읽기 전용 조회용으로만 쓴다.
- 입력은 레거시 `Input` 이 아니라 `UnityEngine.InputSystem` 을 사용한다.
- 폴더/파일 네이밍: 스크립트는 `Mini_<게임이름>/`, 스프라이트는 `0N_<게임이름>/` 규칙을 따른다.

---

# 게임 기획서 (docs/기획.md)

# **\[게임 기획서\] 시대적 맥락 게임잼 프로젝트**

## **프로젝트 기본 개요**

| 항목 | 상세 내용   |
| :---- | :---- |
| **게임 제목** | **추가 작성 필요** |
| **개발 주제** | **“맥락 (Context)”** (세부 포커스: 시대적 맥락) |
| **게임 장르 및 컨셉** | “만져라\! 메이드인 와리오” 컨셉과 유사한 초고속 미니게임 컬렉션 |
| **그래픽 스타일** | 2D의 실제같은(실사풍) 이미지 사용 |
| **조작 방식** | WASD (혹은 방향키) |
| **개발 규모 및 기간** | 4인 팀 / 2박 3일 게임잼 (AI 활용) |
| **타겟 플랫폼 및 엔진** | **Windows, Unity Engine** |

 

## **1\. 게임 컨셉 및 핵심 재미**

**1.1 주제 해석 및 게임 컨셉**

> * 게임잼 주제인 **“맥락(context)”** 중에서도 **“시대적 맥락”**에 포커스를 맞추어 개발.  
> * 시대 별 흐름(선사, 중세, 근대, 현대, 미래)에 따라 스테이지를 분리하고, 각 시대(스테이지) 별로 미니게임을 해결해 엔딩에 도달하는 구조.  
> * “만져라\! 메이드인 와리오” 게임의 컨셉과 유사하게 빠르고 직관적인 미니게임들을 수행함.  
> * 2D의 실제같은(실사) 이미지를 사용하여 독특한 시각적 분위기를 연출함.

**1.2 핵심 재미 요소**

> * **시대별 맞춤형 미니게임 해결:** 선사시대에는 불 붙이기 미니게임, 중세시대에는 마녀 사냥 컨셉으로 마녀를 찾아야 하는 미니게임 등 각 시대 배경에 맞는 참신한 미니게임들을 해결하는 재미.  
> * **역사의 인과관계(맥락) 체험:** 핵심 사건이나 중요 사건의 성공 및 실패 여부에 따라 인류의 역사가 바뀌거나 다음 시대에 영향이 가는 '시대적 맥락'을 관찰하는 흥미.  
> * **기타 세부 핵심 재미 및 연출 기획:** **추가 작성 필요**

 

## **2\. 게임 시스템 기획**

### **2.1 전체 게임 흐름 및 시대 진행**

> * **빅뱅 프롤로그:** 게임을 시작하면 빅뱅부터 시작함. 빅뱅은 첫 번째 미니게임으로, 성공하지 못하면 게임 오버되고 다시 빅뱅부터 시작함. 빅뱅 미니게임을 성공하면 정식으로 게임이 시작하는 느낌을 줌.  
> * **시대 진행 순서:** 빅뱅 성공 후 **\[선사 → 중세 → 근대 → 현대 → 미래\]** 순서대로 스테이지가 진행됨.

### **2.2 미니게임 구성 및 선정 규칙**

> * **미니게임 종류 (3가지):** 일반 미니게임, 변화 미니게임, 핵심 미니게임  
> * **시대별 스테이지 구성:** 각 시대 별로 n개의 미니게임이 존재하며, 그중 **핵심 미니게임 1개 \+ n개 중 랜덤 미니게임 2개를 선택**하여 시대 당 총 3개의 미니게임을 플레이함.  
> * **핵심 미니게임 로직:** 해당 시대에 필수적인 사건에 해당함 (예: 선사시대 불 피우기 미니게임). 실패할 경우 바로 엔딩으로 이동함.  
> * **변화 미니게임 로직:** 근대/현대의 자동차 발명, 컴퓨터 발명과 같이 실패했을 때의 인류를 비교적 상상할 수 있는 사건들. 실패해도 바로 게임이 끝나지 않으며, **다음 시대의 미니게임에 영향을 줌**.

### **2.3 플레이 제한시간 시스템**

> * **타이머 작동:** 플레이 제한시간이 존재하며, 선사 시대에 입장하는 순간부터 카운트가 시작됨.  
> * **타임아웃 로직:** 미래시대까지 완주하기 전 카운트가 종료됐을 때의 로직은 **추가 작성 필요 (아직 미정)**.  
> * **구체적인 제한시간(초/분 단위) 설정:** **추가 작성 필요**

### **2.4 엔딩 크레딧 및 회차 플레이 시스템**

> * **스타워즈풍 엔딩 크레딧:** 미래시대까지 게임을 완료하면 마지막에 스타워즈 인트로처럼 생긴 엔딩 크레딧이 출력됨. 엔딩 크레딧에서는 선사부터 미래까지의 역사를 읊어줌.  
> * **재시작 선택:** 엔딩 크레딧이 끝난 후에는 빅뱅 시대로 다시 돌아가서 게임을 다시 시작할지 선택할 수 있음.

 

## **3\. 플레이어 제어 및 경제 시스템**

### **3.1 플레이어 제어 (조작 방식)**

> * **기본 조작:** **WASD (혹은 방향키)**를 사용하여 조작함.  
> * **시대/미니게임별 상세 조작 및 UI 반응:** **추가 작성 필요**

### **3.2 보상 및 수집 (경제 시스템)**

> * **스티커 획득:** 미니게임을 클리어하면 해당 미니게임의 스티커를 받게 됨. 스티커는 각 미니게임마다 다름.  
> * **갤러리 시스템:** 한 판(미래시대까지 클리어)이 끝났으면, 그 판에서 수집한 미니게임 스티커가 붙어있는 갤러리를 받을 수 있음.  
> * **영구 보존:** 갤러리는 영구히 저장되어 나중에도 확인할 수 있음.  
> * **게임 내 화폐, 상점, 점수 계산 등 기타 경제 시스템:** **추가 작성 필요**

## **4\. AI 활용 계획**

> * **기본 방향:** 4인 팀으로 AI를 사용하여 2박 3일동안 게임을 완성하는 게임잼 프로젝트.  
> * **그래픽 제작:** 2D의 실제같은(실사풍) 이미지를 사용하는 게임이므로, 에셋 생성 및 이미지 구현에 AI를 적극 활용.  
> * **활용할 구체적인 AI 도구 및 모델 (그래픽, 코드, 사운드 등):** **추가 작성 필요**  
> * **게임잼 기간 내 AI 에셋 파이프라인 및 작업 검수 기준:** **추가 작성 필요**

 

## **5\. 개발 마일스톤 및 일정**

> * **개발 규모 및 일정:** 2박 3일 / 4인 팀  
> * **4인 팀원의 역할 분담 (기획, 프로그래밍, 아트 등):** **추가 작성 필요**  
> * **1일차 \~ 3일차 세부 시간대별 마일스톤 및 일정:** **추가 작성 필요**

 

## **6\. 승패 조건 및 특이 사항**

### **6.1 승패 조건 및 분기 매트릭스**

| 조건 및 상황 | 결과 및 시스템 처리 로직   |
| :---- | :---- |
| **빅뱅 미니게임 실패** | 게임 오버되고 **다시 빅뱅부터 시작함**. |
| **빅뱅 미니게임 성공** | 정식으로 게임(선사 시대) 시작. |
| **핵심 미니게임 실패 (예: 선사시대 불 피우기)** | **바로 엔딩으로 이동**하여 해당 사건이 일어나지 않았을 때의 인류를 설명함. *(예: "2026년 현재. 인류는 아직 불을 다루지 못했으며, 수렵채집에 의존하고 동굴에 모여 살고 있다...")* 이후 다시 처음부터 시작할지 종료할지 선택하게 됨. |
| **중요 미니게임 실패 (예: 근/현대 자동차, 컴퓨터 발명)** | 바로 끝나지 않으며, **다음 시대의 미니게임에 영향을 줌**. (구체적으로 어떤 영향을 주는지 상세 로직: **추가 작성 필요**) |
| **플레이 제한시간 종료 (미래 완주 전 카운트 종료)** | **추가 작성 필요 (아직 미정)** |
| **미래 시대까지 완료 (최종 승리)** | 스타워즈 인트로 형식의 역사 서사 엔딩 크레딧 출력 → 해당 판에서 수집한 스티커가 붙은 갤러리 영구 저장 → 크레딧 종료 후 빅뱅 시대로 돌아가 재시작 선택 가능. |

### **6.2 특이 사항**

> * 아이디어에 명시된 내용 외 게임 밸런싱, 기술적 제약, 해상도 및 사양 등 기타 특이 사항: **추가 작성 필요**
</content>
</invoke>
